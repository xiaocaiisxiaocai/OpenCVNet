<UserControl x:Class="VisionInspection.App.Views.RunView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="200" />
        </Grid.RowDefinitions>

        <!-- Row0 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <ui:Button Command="{Binding ConnectCommand}" Appearance="Secondary" Margin="0,0,6,0" Padding="12,8">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="PlugConnected24" Margin="0,0,6,0" />
                    <TextBlock Text="连接设备" VerticalAlignment="Center" />
                </StackPanel>
            </ui:Button>
            <ui:Button Command="{Binding DisconnectCommand}" Appearance="Secondary" Padding="12,8">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="PlugDisconnected24" Margin="0,0,6,0" />
                    <TextBlock Text="断开" VerticalAlignment="Center" />
                </StackPanel>
            </ui:Button>

            <Border Width="1" Background="#26FFFFFF" Margin="12,4" />

            <ui:Button Command="{Binding StartCommand}" Appearance="Primary" Margin="0,0,6,0" Padding="12,8">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Play24" Margin="0,0,6,0" />
                    <TextBlock Text="启动运行" VerticalAlignment="Center" />
                </StackPanel>
            </ui:Button>
            <ui:Button Command="{Binding StopCommand}" Appearance="Secondary" Margin="0,0,6,0" Padding="12,8">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Stop24" Margin="0,0,6,0" />
                    <TextBlock Text="停止" VerticalAlignment="Center" />
                </StackPanel>
            </ui:Button>
            <ui:Button Command="{Binding SimulateTriggerCommand}" Appearance="Secondary" Margin="0,0,6,0" Padding="12,8"
                       Visibility="{Binding CanSimulate, Converter={StaticResource BoolToVisibility}}">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Flash24" Margin="0,0,6,0" />
                    <TextBlock Text="模拟触发" VerticalAlignment="Center" />
                </StackPanel>
            </ui:Button>
            <ui:Button Command="{Binding ResetStatsCommand}" Appearance="Secondary" Padding="12,8">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="ArrowClockwise24" Margin="0,0,6,0" />
                    <TextBlock Text="清零统计" VerticalAlignment="Center" />
                </StackPanel>
            </ui:Button>
        </StackPanel>

        <!-- Row1 状态栏 -->
        <ui:Card Grid.Row="1" Margin="0,0,0,10" Padding="14,10">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border CornerRadius="14" Padding="10,5" Margin="0,0,10,0" Background="#1AFFFFFF">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Camera24" Margin="0,0,6,0" />
                            <TextBlock Text="相机" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <Ellipse Width="10" Height="10" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="#7F8C8D" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CameraConnected}" Value="True">
                                                <Setter Property="Fill" Value="#4CAF50" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </StackPanel>
                    </Border>

                    <Border CornerRadius="14" Padding="10,5" Margin="0,0,10,0" Background="#1AFFFFFF">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="DeveloperBoard24" Margin="0,0,6,0" />
                            <TextBlock Text="PLC" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <Ellipse Width="10" Height="10" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="#7F8C8D" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding PlcConnected}" Value="True">
                                                <Setter Property="Fill" Value="#4CAF50" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </StackPanel>
                    </Border>

                    <Border CornerRadius="14" Padding="10,5" Margin="0,0,16,0" Background="#1AFFFFFF">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Pulse24" Margin="0,0,6,0">
                                <ui:SymbolIcon.Style>
                                    <Style TargetType="ui:SymbolIcon">
                                        <Setter Property="Foreground" Value="#7F8C8D" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Heartbeat}" Value="True">
                                                <Setter Property="Foreground" Value="#FF5252" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ui:SymbolIcon.Style>
                            </ui:SymbolIcon>
                            <TextBlock Text="心跳" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>

                    <TextBlock Text="型号" VerticalAlignment="Center" Opacity="0.6" />
                    <TextBlock Text="{Binding CurrentModel}" VerticalAlignment="Center" FontWeight="Bold" FontSize="15" Margin="6,0,20,0" />
                    <TextBlock Text="节拍" VerticalAlignment="Center" Opacity="0.6" />
                    <TextBlock Text="{Binding LastCycleMs, StringFormat={}{0} ms}" VerticalAlignment="Center" FontWeight="Bold" Margin="6,0,0,0" />
                </StackPanel>

                <Border HorizontalAlignment="Right" CornerRadius="8" Padding="22,6" MinWidth="96"
                        Background="{Binding CurrentOutcomeBrush}">
                    <TextBlock Text="{Binding CurrentOutcome}" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />
                </Border>
            </Grid>
        </ui:Card>

        <!-- Row2 主体 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="380" />
            </Grid.ColumnDefinitions>

            <!-- 实时图像 -->
            <ui:Card Grid.Column="0" Margin="0,0,10,0">
                <Grid>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                                Visibility="{Binding CurrentImage, Converter={StaticResource NullToVisibility}}">
                        <ui:SymbolIcon Symbol="Image24" FontSize="52" Opacity="0.22" HorizontalAlignment="Center" />
                        <TextBlock Text="等待图像…" Opacity="0.5" Margin="0,12,0,0" HorizontalAlignment="Center" FontSize="14" />
                        <TextBlock Text="连接设备 → 模拟触发一次" Opacity="0.35" FontSize="11" Margin="0,4,0,0" HorizontalAlignment="Center" />
                    </StackPanel>
                    <Viewbox Stretch="Uniform" Margin="6">
                        <Grid Width="{Binding ImageWidth}" Height="{Binding ImageHeight}">
                            <Image Source="{Binding CurrentImage}" Stretch="Fill" />
                            <ItemsControl ItemsSource="{Binding Overlays}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate><Canvas /></ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Width="{Binding Width}" Height="{Binding Height}">
                                            <Rectangle Stroke="{Binding Stroke}" StrokeThickness="3" />
                                            <Border Background="{Binding Stroke}" HorizontalAlignment="Left" VerticalAlignment="Top" Padding="4,1">
                                                <TextBlock Text="{Binding Label}" Foreground="White" FontSize="13" FontWeight="Bold" />
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Viewbox>
                </Grid>
            </ui:Card>

            <!-- 右面板 -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <UniformGrid Grid.Row="0" Columns="2" Margin="0,0,0,10">
                    <ui:Card Margin="0,0,5,5" Padding="14">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="DataHistogram24" Opacity="0.6" Margin="0,0,6,0" />
                                <TextBlock Text="总数" Opacity="0.6" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding Total}" FontSize="26" FontWeight="Bold" Margin="0,6,0,0" />
                        </StackPanel>
                    </ui:Card>
                    <ui:Card Margin="5,0,0,5" Padding="14">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="DataPie24" Opacity="0.6" Margin="0,0,6,0" />
                                <TextBlock Text="良率" Opacity="0.6" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding YieldRate, StringFormat={}{0:P1}}" FontSize="26" FontWeight="Bold" Margin="0,6,0,0" />
                        </StackPanel>
                    </ui:Card>
                    <ui:Card Margin="0,5,5,0" Padding="14">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="CheckmarkCircle24" Foreground="#4CAF50" Margin="0,0,6,0" />
                                <TextBlock Text="OK" Opacity="0.6" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding Ok}" FontSize="26" FontWeight="Bold" Foreground="#4CAF50" Margin="0,6,0,0" />
                        </StackPanel>
                    </ui:Card>
                    <ui:Card Margin="5,5,0,0" Padding="14">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="DismissCircle24" Foreground="#F44336" Margin="0,0,6,0" />
                                <TextBlock Text="NG" Opacity="0.6" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding Ng}" FontSize="26" FontWeight="Bold" Foreground="#F44336" Margin="0,6,0,0" />
                        </StackPanel>
                    </ui:Card>
                </UniformGrid>

                <ui:Card Grid.Row="1">
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                            <ui:SymbolIcon Symbol="TextBulletListSquare24" Margin="0,0,6,0" />
                            <TextBlock Text="逐工位结果" FontWeight="SemiBold" VerticalAlignment="Center" />
                        </StackPanel>
                        <DataGrid ItemsSource="{Binding StationResults}" AutoGenerateColumns="False"
                                  CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column"
                                  Background="Transparent" BorderThickness="0" GridLinesVisibility="Horizontal">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="工位" Binding="{Binding Index}" Width="50" />
                                <DataGridTextColumn Header="状态" Binding="{Binding State}" Width="80" />
                                <DataGridTextColumn Header="得分" Binding="{Binding Score}" Width="*" />
                            </DataGrid.Columns>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Ok}" Value="False">
                                            <Setter Property="Background" Value="#33F44336" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>
                    </DockPanel>
                </ui:Card>
            </Grid>
        </Grid>

        <!-- Row3 报警 | 日志 -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:Card Grid.Column="0" Margin="0,0,5,0">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                        <ui:SymbolIcon Symbol="Warning24" Foreground="#FFB300" Margin="0,0,6,0" />
                        <TextBlock Text="报警" FontWeight="SemiBold" VerticalAlignment="Center" />
                    </StackPanel>
                    <ListBox ItemsSource="{Binding Alarms}" Background="Transparent" BorderThickness="0" />
                </DockPanel>
            </ui:Card>
            <ui:Card Grid.Column="1" Margin="5,0,0,0">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                        <ui:SymbolIcon Symbol="DocumentText24" Margin="0,0,6,0" />
                        <TextBlock Text="日志" FontWeight="SemiBold" VerticalAlignment="Center" />
                    </StackPanel>
                    <ListBox ItemsSource="{Binding Logs}" Background="Transparent" BorderThickness="0" />
                </DockPanel>
            </ui:Card>
        </Grid>

        <!-- 报警瞬时提示（覆盖全区，底部弹出，不遮挡布局） -->
        <ui:SnackbarPresenter x:Name="RootSnackbar" Grid.RowSpan="4" />
    </Grid>
</UserControl>
